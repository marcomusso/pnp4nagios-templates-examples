define page {
       use_regex 1		# 0 = use no regular expressions, 1 = use regular expressions
       page_name OracleiSITE2-CONNTIME	# page description
}

define graph {
       host_name       .+\.site2\.priv
       service_desc    .+-ConnectionTime
}
